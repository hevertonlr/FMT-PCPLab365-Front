import { ValidationStyleDirective } from './validation-style.directive';

describe('ValidationStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationStyleDirective();
    expect(directive).toBeTruthy();
  });
});
